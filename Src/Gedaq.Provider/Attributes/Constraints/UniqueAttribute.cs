namespace Gedaq.Provider.Attributes.Constraints
{
    public sealed class UniqueAttribute : ConstrainAttribute
    {
        public UniqueAttribute(string constrainName) : base(constrainName)
        {
        }
    }
}