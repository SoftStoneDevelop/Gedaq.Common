namespace Gedaq.Provider.Enums
{
    public enum ResultType : byte
    {
        Single = 0,
        FirstOrDefault = 1,
        List = 2,
        Array = 3,
        HashSet = 4,
        Enumerable = 5
    }
}